git status
git add .
git commit -m "Completed Lesson 04 - CSS box model"
git push
git log

#branching and merging
git branch #tells you which branch you are at
git branch 'label' #creates a new branch
git switch 'label' #swtiches to that branch
git merge 'label' #merges the branch with the branch you are on